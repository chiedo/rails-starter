/** @jsx React.DOM */
var React = require('react');

var Bus = React.createClass({

  render: function() {
    return (
      <div className="bus">
        Bus without wheels.
      </div>
    );
  }

});

module.exports = Bus;
