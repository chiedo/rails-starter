/** @jsx React.DOM */
var React = require('react');

var Wheel = React.createClass({

  render: function() {
    return (
      <div className="wheel">
        Wheel
      </div>
    );
  }

});

module.exports = Wheel;
