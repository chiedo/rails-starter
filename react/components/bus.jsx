var React = require('react');

var Bus = React.createClass({

  render: function() {
    return (
      <div className="bus">
        A Bus without wheels.
      </div>
    );
  }

});

module.exports = Bus;
